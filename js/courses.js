jQuery( document ).ready( function( $ ) {
	// we can use $() inside here instead of jQuery()
	
	
	$( '#addCourse' ).click( function() {
		// put code here to handle the button click
		alert( 'I got clicked!' );
	});
	
	
});